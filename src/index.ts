/**
 * custom-er — public entry point.
 *
 * Components ship as JS only; consumers must also load the token contract and
 * have Tailwind configured to scan this package. See the README for setup.
 */
export * from './components/Button'
export * from './theme'
